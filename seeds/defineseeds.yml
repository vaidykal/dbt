version: 2

seeds:
  - name: CountryDialCode
    config:
     column_types:
       COUNTRY: varchar(30)
       COUNTRY_CODE: Number
       DialCodeSubDir:
        DialCode2:
          column_types:
           COUNTRY: varchar(30)
           COUNTRY_CODE: Number